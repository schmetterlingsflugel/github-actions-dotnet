name: Run every 5 minutes

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  cron:
    name: Run every 5 minutes
    runs-on: ubuntu-latest

    steps:
     - name: Hello World
       run: echo "Hello World"
